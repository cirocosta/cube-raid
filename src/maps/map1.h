#ifndef MAPS_H
#define MAPS_H

#include "../lib/queue.h"
#include "../lib/tipos.h"

/**
 * Gera um mapa - feito diretamente em codigo, inicialmente.
 * @return Queue representando o mapa.
 */
Queue MAPS_generate_map();
#endif
